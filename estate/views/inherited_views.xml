<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Add real estate offers tab to res.partner form -->
    <record id="res_partner_view_form_inherited" model="ir.ui.view">
        <field name="name">res.partner.view.form.inherited.estate</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <notebook position="inside">
                <page string="Real Estate Offers">
                    <field name="offer_ids">
                        <tree>
                            <field name="property_id"/>
                            <field name="price"/>
                            <field name="status"/>
                            <field name="date_deadline"/>
                        </tree>
                    </field>
                </page>
            </notebook>
        </field>
    </record>

    <!-- Add properties tab to res.users form -->
    <record id="res_users_view_form_inherited" model="ir.ui.view">
        <field name="name">res.users.view.form.inherited.estate</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_form"/>
        <field name="arch" type="xml">
            <notebook position="inside">
                <page string="Real Estate Properties">
                    <field name="property_ids">
                        <tree>
                            <field name="name"/>
                            <field name="expected_price"/>
                            <field name="date_availability"/>
                            <field name="state"/>
                        </tree>
                    </field>
                </page>
            </notebook>
        </field>
    </record>

</odoo>